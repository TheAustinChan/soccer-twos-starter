bryanoliveira/soccer-twos-env: A Python package to wrap a compiled soccer-twos reinforcement learning environment with gym-compatible interfaces.

Action:
Affect the wrapper -> implement in utils -> train and get checkpoints -> modify agent to run on checkpoints with new state vars

Wrapper: 
Affect reward: add reward to 
maximize a ratio between closeness of ball to enemy goal and minimize ratio of closeness of ball to home goal. Hierarchy of rewards, expect which rewards to increase linearly or by multiplies or quadratic or logistically.’
Plan: add to reward by 
Get rewards from sent info of decision & terminal steps from self.env.get_steps(self.name)
Affect observation to decrease sparsity of rewards so we get rewards more often have more data:
Defender 0 minimize dist id 0 from goal position -> learn what the environment looks like in terms of spacing, 
How do we get collision or infer collision,  observation can add in prior ball info

Rewards:

Ball possession
Blocking
Passing
Scoring by default 
Attacker gets close to ball
Attacker possesses the ball to avoid enemy
Minimize team conflict of ball possesion
Pass when blocked by an enemy
simple passing is just moving towards

pass_encouraging_reward = (dist(member we assume close to enemy, its closest enemy) + dist(member without ball further from an enemy, its closest enemy)) something like this
to encourage highest reward for when we want to pass

Penalty for holding ball without shooting or passing

Need to minimize as well the amount of extra rewarding we do or rank them by weight
Weight higher towards special actions like pass, avoid
Reward heirarchy have to rank the importance of each reward that we have, perhaps along some function or something
